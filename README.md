# NEXALVO v1.6.2 — Phase 5A Loyalty UI Fix

Frontend-only hotfix based on v1.6.1.

- Keeps the validated Supabase Loyalty backend and existing customer loyalty ledger intact.
- Makes the manual loyalty point quantity field clearly visible and labeled `Points`.
- Uses a responsive two-column layout on wider screens and stacked fields on narrow screens.
- No SQL changes are required for this hotfix.

Build marker: `v1.6.2`.
